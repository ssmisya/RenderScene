# Git 版本管理

远端：`git@github.com:ssmisya/RenderScene.git`。默认主版本为 **V2**，主分支为 **main**，首个固定版本标签为 **v2.0.0**，版本号写在 `VERSION`。

## 获取完整工程

先按照 [Git LFS 官方说明](https://git-lfs.com/) 安装 Git LFS，然后运行：

```sh
git lfs install
git clone git@github.com:ssmisya/RenderScene.git
cd RenderScene
git lfs pull
```

下载 LFS 实体文件后才能打开 Blender、图片与 GLB。优先使用上述克隆方式；GitHub 普通源码 ZIP 可能只包含 LFS 指针。打开根目录 `Harbin_Sophia_Square.blend`，具体操作见 README。

## 当前本机工作目录

所有操作继续在用户指定的 iCloud 工程目录进行：

```sh
cd '/Users/songmingyang/Library/Mobile Documents/com~apple~CloudDocs/Desktop/projects/Local tasks/blender'
git status
git pull --ff-only
```

本机的 Git LFS 3.8.0 安装在被忽略的 `.tools/git-lfs`；此仓库过滤器、`git lfs` 别名与本地推送钩子已配置。保留原有全局推送检查，它通过 `.git/hooks/pre-push` 接入 LFS。普通 `git add` / `git push` 可直接使用。其他电脑按官方方式安装 Git LFS。移动本机目录后需更新本地过滤器、别名与钩子中的绝对路径。

## 日常修改

修改前先看 `git status`；存在未提交改动时先提交或妥善暂存，再拉取更新。较大修改使用独立分支：

```sh
git switch -c codex/improve-street-detail
# 编辑、保存并检查工程
git add -A
git diff --cached --stat
git commit -m "Improve street facade details"
git push -u origin codex/improve-street-detail
```

检查后通过 Pull Request 合入 `main`。Blender 二进制文件不能逐行合并，多人工作时避免同时修改同一主文件。

## 发布与回看

修正版本使用 `2.0.1`、`2.0.2`，较大功能更新使用 `2.1.0`。更新 `VERSION` 和 `CHANGELOG.md`，提交到 main 后建立新标签：

```sh
git tag -a v2.0.1 -m "V2.0.1: describe the changes"
git push origin main
git push origin v2.0.1
```

保留已发布标签，不移动、不覆盖。查看初始 V2 前先保存当前改动：

```sh
git switch --detach v2.0.0
git lfs pull
# 查看完成后回到主线
git switch main
```

V1 位于 `backups/pre_v2_20260913/` 及原有 ZIP、GLB；未人为伪造早期 Git 提交。

## 同步范围与校验

主工程、脚本、贴图、照片、渲染、游戏导出、历史 ZIP 与备份纳入版本管理。仅忽略系统杂项、日志、Python 缓存、Blender 自动备份和本机工具；它们仍保留在本地。

`SYNC_SNAPSHOT_V2.json` 保存首次同步时项目文件的 SHA-256 和大小。以后新增版本以对应 Git 提交与 LFS 对象为准。原 `MANIFEST_v2.json` 和打包报告属于此前的 V2 ZIP，保留历史含义。根目录 blend 在原打包后再次保存，因此与 ZIP 内版本哈希不同。

```sh
git status --short
git log --oneline --decorate -10
git lfs ls-files
git lfs fsck
```
